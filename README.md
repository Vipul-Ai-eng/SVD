# Mathematical Overview

Given a matrix A, SVD factorizes it into:

A = U Σ Vᵀ

Where:

U = left singular vectors

Σ = diagonal matrix of singular values

V = right singular vectors

This project shows how each of these components is derived manually.
